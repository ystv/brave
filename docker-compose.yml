version: "3"

services:
  brave:
    image: pog
    #build: .
    ports:
      - "5000:5000"
    restart: always

  caddy:
    image: caddy
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    ports:
      - "8443:8443"
    depends_on:
      - brave
    restart: always
